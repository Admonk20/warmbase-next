Update `src/lib/ai.server.ts` line 49: change the default Kimi model from `kimi-k2-turbo-preview` to `kimi-k2.5`. Also update the temperature-forcing check (currently special-cases `kimi-k2.6`) to apply to `kimi-k2.5` as well, since Moonshot's reasoning-class models reject any temperature other than 1.

Heads-up: `k2.5` is a heavier reasoning model than `k2-turbo-preview`. Calls may take longer and could re-trigger the upstream timeout that prompted the earlier switch to turbo. If that happens, the real fix is a job-queue pattern rather than another model swap.